# 记账 App 开发起步方案（PLAN v1.0）

> 项目代号：cash-diary ｜ 目标平台：Android 手机（预留微信小程序）
> 本方案定位：**初学者友好、小步快跑、先本地后云端**

---

## 0. 方案总览

| 维度 | 推荐结论 | 理由 |
|---|---|---|
| 技术栈 | **uni-app（Vue3 + Vite）** | 接得上你正在学的 Vue3；一套代码出 Android APK + 微信小程序；中文文档与社区对初学者最友好 |
| 数据存储 | 本地 **SQLite** 为唯一事实源 | 离线优先，没网也能记，云同步只是后续增强 |
| 同步方案 | 阶段4 引入 **uniCloud**（备选：自建 FastAPI） | 免运维、有免费额度；你熟悉 FastAPI，可作为进阶备选 |
| 开发节奏 | 5 个阶段，约 6~8 周业余完成 MVP | 每阶段有独立可验收的产物，随时可用 |

> **2026-09-24 更新**：MVP 范围已与用户确认并冻结，详见 `docs/PRD.md`（产品设计文档）；可交互原型见 `demo/index.html`（HTML 模拟数据，用于验证界面与操作流程，正式开发将按本方案迁移为 uni-app）。

---

## 1. 核心功能规划（按优先级）

### P0 —— MVP 最小可用（没有它就不算记账 App）
| 功能 | 说明 | 验收标准 |
|---|---|---|
| 快速记账 | 金额 + 收支类型 + 分类 + 备注 + 时间，一步提交 | 3 秒内完成一笔账 |
| 收支分类管理 | 内置常用分类（餐饮/交通/工资…），可增删改、可排序 | 分类 CRUD 完整 |
| 流水列表 | 按时间倒序展示，支持删除 | 记完立刻能在列表看到 |
| 本地持久化 | 杀掉 App 重开数据还在 | 重启数据不丢 |
| 账本编辑 | 可修改、可删除单条流水 | 改金额后统计联动正确 |

### P1 —— 让数据产生价值
| 功能 | 说明 |
|---|---|
| 月度统计 | 当月收支合计、分类占比（饼图）、近 6 个月趋势（柱状图） |
| 预算设置 | 月度总预算 + 可选分类预算 |
| 预算提醒 | 超支 80% / 100% 时本地通知提醒 |
| 按月筛选 | 流水页支持切换月份 |

### P2 —— 体验与安全感
| 功能 | 说明 |
|---|---|
| 数据导出 | CSV 导出（可发微信/邮件存档） |
| 数据备份/恢复 | JSON 整库导出 + 导入 |
| 多账本 | 如"日常"和"生意"分开记 |
| 搜索与筛选 | 按关键词、分类、金额区间筛选 |
| 深色模式 | 跟随系统 |

### P3 —— 远期（先不做，只留扩展口）
云同步多端、多人共享账本、账户/资产管理（信用卡、负债）、账单图片附件。

---

## 2. 技术选型详解

### 2.1 跨平台方案对比

| 方案 | 语言 | 上手难度 | 能出什么 | 优点 | 缺点 | 适合你吗 |
|---|---|---|---|---|---|---|
| **uni-app** | Vue3 | ★★☆ | Android/iOS App + 微信小程序 + H5 | 中文文档、生态插件多、与你 Vue3 学习路线重合 | 性能中等、绑定 DCloud 生态 | ✅ **首选** |
| Flutter | Dart | ★★★ | Android/iOS App | 性能好、跨端一致 | 要学 Dart、包体较大 | 备选（想深耕客户端方向时） |
| React Native | JS/React | ★★★ | Android/iOS App | 复用 React | 环境配置复杂、国内生态一般 | 备选 |
| 原生 Android | Kotlin | ★★★★ | 仅 Android | 性能最佳、学到底层 | 周期长、不能出小程序 | 不推荐起步用 |
| PWA 网页版 | HTML/CSS/JS | ★☆ | 网页（可"添加到主屏"） | 零新知识 | 体验弱、能力受限 | 可作练手，不作正式方案 |

### 2.2 开发工具

- **编辑器**：HBuilderX（DCloud 官方 IDE，创建项目、真机运行、云打包一站式；uni-app 对它有最好支持）
- **备选**：任意编辑器 + CLI（`npm create uni@latest`，Vite 工程化，适合熟悉命令行后）
- 本机 Node 22 已就绪，无需额外安装环境。

### 2.3 同步后端对比（阶段4 才需要）

| 方案 | 说明 | 优点 | 缺点 |
|---|---|---|---|
| **uniCloud**（推荐） | DCloud  serverless 云，自带云数据库 + 登录（微信/手机号） | 零运维、免费额度够个人用、前端直接调 | 绑定 DCloud 生态 |
| 自建 FastAPI + MySQL | 你已熟悉的技术栈，可部署阿里云 | 完全可控、顺便练后端 | 要自己运维、写接口、做安全 |
| WorkBuddy 云开发 | 现成托管后端（数据库/登录/存储） | 配置快 | 生态新、社区资料少 |

---

## 3. 数据存储与同步思路

### 3.1 核心原则：离线优先（Offline-first）

把本地数据库想象成**账本原件**，云端只是**放在保险柜里的复印件**：

- 一切读写先落在本地 SQLite，界面永远读本地 → 没网、弱网都能用，速度最快
- 同步引擎在后台把本地变更"抄送"到云端；换设备时从云端"取回复印件"
- 永远不要出现"没网就记不了账"或"界面卡在等网络"的情况

### 3.2 MVP 数据模型（SQLite）

```sql
-- 分类（内置 + 自定义）
CREATE TABLE category (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  name        TEXT    NOT NULL,             -- 如"餐饮"
  type        TEXT    NOT NULL,             -- 'income' | 'expense'
  icon        TEXT    DEFAULT '',           -- 图标名
  sort        INTEGER DEFAULT 0,
  enabled     INTEGER DEFAULT 1
);

-- 账本（P2 多账本用，MVP 只建一条默认账本）
CREATE TABLE account (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  name        TEXT    NOT NULL,
  created_at  INTEGER NOT NULL              -- 毫秒时间戳
);

-- 流水（核心表）
CREATE TABLE transaction_record (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  account_id   INTEGER NOT NULL,
  category_id  INTEGER NOT NULL,
  type         TEXT    NOT NULL,            -- 'income' | 'expense'
  amount_cents INTEGER NOT NULL,            -- 金额，单位：分！
  note         TEXT    DEFAULT '',
  occurred_at  INTEGER NOT NULL,            -- 发生时间（毫秒时间戳）
  created_at   INTEGER NOT NULL,
  updated_at   INTEGER NOT NULL,
  deleted_at   INTEGER                      -- 软删除：同步用，NULL 表示未删
);

-- 预算（P1）
CREATE TABLE budget (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  account_id   INTEGER NOT NULL,
  category_id  INTEGER,                     -- NULL = 月度总预算
  month        TEXT    NOT NULL,            -- '2026-09' 格式，方便按月查
  limit_cents  INTEGER NOT NULL
);
```

### 3.3 三条铁律

1. **钱用整数分存储**。`19.9 元` 存 `1990`。浮点数 `0.1 + 0.2 ≠ 0.3`，月底对账差一分钱能查一晚上。显示时才格式化成 `¥19.90`。
2. **时间存毫秒时间戳**，显示时按**手机本地时区**格式化；月度统计按"用户本地日历月"分组，不要用 UTC 月份。
3. **同步用软删除**（`deleted_at`），物理删除只发生在本地清理时——否则一端删了、另一端又同步回来，幽灵数据会反复出现。

### 3.4 同步策略演进（不要跳步）

```
阶段1-3：纯本地，只做"导出 JSON/CSV 备份"（人工保险柜）
阶段4  ：uniCloud 账号登录 + 自动同步
         - 每条记录带 updated_at，同步=双向比对"谁更新"取新者（last-write-wins）
         - 同步是"幂等"的：同一条记录重复同步 N 次结果不变
         - 冲突少见（个人单机为主），先取新者，够用
阶段5+ ：需要多人协作时再上"向量时钟/操作日志"等高级冲突解决
```

---

## 4. 项目架构设计

### 4.1 分层结构（从上到下，上层只依赖下层）

```
┌─────────────────────────────────────────────┐
│ 页面层 pages/                                 │  记账页、流水页、统计页、设置页
├─────────────────────────────────────────────┤
│ 状态层 stores/（Pinia）                       │  流水列表、分类、预算的响应式状态
├─────────────────────────────────────────────┤
│ 业务层 services/                              │  记账规则、预算校验、月度聚合计算
├─────────────────────────────────────────────┤
│ 数据层 db/（仓储层）                           │  SQL 封装：增删改查、事务、迁移
├─────────────────────────────────────────────┤
│ 存储适配 adapter/                             │  SQLite 实现（未来可加云端适配器）
└─────────────────────────────────────────────┘
```

**为什么分层**（初学者记住一个类比）：
- 页面层是**餐厅大堂**，只负责点菜上菜（展示、收输入）
- 业务层是**后厨菜谱**，规定"记账必须校验金额>0"这类规则
- 数据层是**仓库管理员**，只有它碰货架（数据库）
- 好处：换存储方案（SQLite → 云端）时，只换仓库管理员，大堂和后厨不用动

### 4.2 建议目录结构（uni-app 工程）

```
记账app/
├─ pages/                  # 页面
│  ├─ quick-add/           # 快速记账（首页）
│  ├─ transactions/        # 流水列表
│  ├─ stats/               # 月度统计
│  └─ settings/            # 设置（分类管理、预算、备份）
├─ components/             # 可复用组件（金额键盘、分类选择器、流水卡片）
├─ stores/                 # Pinia 状态
├─ services/               # 业务逻辑
├─ db/
│  ├─ schema.sql           # 建表语句
│  ├─ migrations/          # 版本升级脚本（v1→v2…）
│  └─ repository/          # 各表 CRUD
├─ utils/                  # 金额格式化、日期工具
├─ static/                 # 图标等资源
└─ tests/                  # 单元测试
```

### 4.3 关键设计决策

| 决策 | 选择 | 原因 |
|---|---|---|
| 状态管理 | Pinia | Vue3 官方推荐，比 Vuex 简单 |
| 图表 | uCharts / qiun-data-charts | uni-app 生态内最成熟，支持饼图/柱状图 |
| 数据库 | plus.sqlite（App 端原生 SQLite） | 数据量大、可 SQL 聚合，性能远超 localStorage |
| 本地通知 | uni-app 自带通知 API（P1 预算提醒用） | 无需第三方 |
| 金额输入 | 自定义数字键盘组件 | 避免系统键盘遮挡，体验好 |

---

## 5. 分阶段开发路线图

> 时间按"业余每天 1~2 小时"估算，可按进度伸缩。每阶段结束都是一个**能用、可验收**的版本。

### 阶段 0：环境搭建与项目骨架（0.5~1 天）【P0】
**任务**
1. 安装 HBuilderX（官网下载，免安装解压即用）
2. 创建 uni-app 项目（Vue3 + Vite 版），关联本仓库目录
3. 真机跑通：手机开 USB 调试 → HBuilderX"运行 → 运行到手机"→ 看到欢迎页
4. 初始化 git 提交 + 搭建目录骨架

**验收**：自己手机上出现一个能打开的空白 App。

**最容易踩的坑**
- 手机连上电脑但识别不到 → 安卓要开"开发者选项→USB 调试"，部分机型（小米/华为）还要单独开"USB 安装"和装驱动
- HBuilderX 创建的工程落在别处，git 仓库对不上 → 让工程根目录就是本仓库目录（或反过来，把工程文件挪进来）

**必须掌握的知识**
- 安卓"开发者模式/USB 调试"是什么（手机的"后门钥匙"）
- uni-app 项目的页面注册机制（pages.json）

---

### 阶段 1：MVP 核心记账（1.5~2 周）【P0】
**任务**
1. 建库建表（schema.sql），写数据库封装层
2. 内置分类数据（餐饮/购物/交通/住房/工资/红包等 10 个左右）
3. 快速记账页：自定义数字键盘 + 分类选择 + 提交
4. 流水列表页：倒序展示、下拉刷新
5. 编辑/删除流水

**验收**：连续记 10 笔账，杀进程重开，数据完好。

**最容易踩的坑**
- **金额用浮点数存** → 后期对账差分（本方案最大坑，见 §3.3 铁律1）
- 用 localStorage 存流水 → 数据量稍大就卡、且无 SQL 能力
- 时间直接存字符串 `"2026-09-24"` → 排序、按月统计时格式地狱
- 删除直接用 SQL `DELETE` → 为同步埋雷（应软删除）

**必须掌握的知识**
- SQLite 基础 SQL：INSERT/SELECT/UPDATE、ORDER BY
- 毫秒时间戳与日期格式化（时区概念）
- Pinia：列表数据如何响应式更新

---

### 阶段 2：统计图表与预算（1.5~2 周）【P1】
**任务**
1. SQL 聚合：按月分组求和、按分类分组求和
2. 统计页：当月收支合计 + 分类饼图 + 6 个月趋势柱状图（uCharts）
3. 预算设置页（总预算 + 分类预算）
4. 预算校验逻辑 + 超支本地通知

**验收**：能看到"本月花了多少、花在哪"，超预算时收到提醒。

**最容易踩的坑**
- 在 JS 里循环算合计 → 数据量大了卡；聚合交给 SQL 的 `GROUP BY`
- 月度统计差一天 → 时区/日期边界问题（铁律2）
- 饼图分类太多挤成一团 → 只展示 Top 6，其余合并为"其他"
- 通知在模拟器正常、真机不响 → 安卓 8.0+ 必须配"通知渠道(channel)"

**必须掌握的知识**
- SQL `GROUP BY` + `SUM()` 聚合
- 图表组件的数据格式（categories/series 两个数组）
- 安卓通知渠道机制

---

### 阶段 3：体验打磨与数据安全（1 周）【P2】
**任务**
1. CSV 导出、JSON 备份/恢复（文件分享）
2. 多账本支持（建第二条 account 记录 + 切换）
3. 搜索筛选（关键词/分类/金额区间）
4. 深色模式、空状态插画、加载态

**验收**：换个手机，靠 JSON 备份把数据完整搬过去。

**最容易踩的坑**
- 恢复备份时主键冲突 → 恢复策略选"覆盖"或"重映射 id"，想清楚再动手
- CSV 用 Excel 打开中文乱码 → 加 BOM 头（`\uFEFF`）
- 备份文件越滚越大 → 流水表加分页查询（LIMIT/OFFSET 或按月份拉取）

**必须掌握的知识**
- 文件读写与系统分享（uni 的 share API）
- 数据库分页查询

---

### 阶段 4：账号与云同步（2~3 周）【P3 可选，建议 MVP 稳定后做】
**任务**
1. uniCloud 开通 + 微信/手机号登录
2. 同步引擎：本地变更队列 → 推云端 → 拉远端新变更 → 按 updated_at 合并
3. 软删除同步、首次登录全量对账

**验收**：A 手机记账 → B 手机登录同一账号能看到。

**最容易踩的坑**
- 不做变更队列，直接"全量上传覆盖" → 网速差时丢数据、费流量
- 多端同时改一条 → 冲突覆盖丢更新（先 last-write-wins，够用）
- 同步接口重复执行产生重复记录 → 每条记录要有稳定 id，同步必须幂等

**必须掌握的知识**
- "幂等"概念（同一个操作执行多次结果一样，就像电梯按钮按几次都只来一次）
- 登录态/token 的基本原理
- 简单冲突解决策略

---

### 阶段 5：测试与发布（1~2 周）【P2】
**任务**
1. 单元测试：金额计算、月度聚合、预算判断（vitest）
2. 手工测试清单：记账→统计→备份→恢复全链路过一遍
3. Android 云打包（生成签名 APK）→ 自用安装
4. （可选）申请微信小游戏/小程序类目 → 小程序版上线

**验收**：手机里装着自己签名的 APK，稳定用一周。

**最容易踩的坑**
- 云打包要 DCloud 账号 + 实名，且排队耗时 → 提前注册认证
- 国内上架应用市场需要《隐私政策》+ 工信部合规（权限最小化、SDK 清单）→ 自用 APK 可跳过，上架不能跳
- 没有签名的 APK 无法覆盖安装 → 证书 keystore 要备份好，丢了就永远覆盖不了旧版

**必须掌握的知识**
- 安卓签名证书原理（就是"开发者身份证"）
- 版本号管理（versionCode 递增才能升级）

---

## 6. 全局避坑清单 & 知识地图

### 6.1 五个最大的坑（按血泪程度排序）

| # | 坑 | 一句话对策 |
|---|---|---|
| 1 | 金额用浮点数 | 一律整数"分"，显示层才格式化 |
| 2 | 上来就做云同步 | 先本地跑稳 2 周，同步后置 |
| 3 | 时间存字符串/忽略时区 | 毫秒时间戳 + 本地时区格式化 |
| 4 | 用 localStorage 当数据库 | 流水必须进 SQLite |
| 5 | 物理删除记录 | 软删除（deleted_at），给同步留后路 |

### 6.2 各阶段必学知识地图

```
SQL 基础（阶段1必须） ──→ SQL 聚合 GROUP BY（阶段2）
Vue3 组件与响应式 ──→ Pinia（阶段1-2）
时间戳/时区处理（阶段1） ──→ 安卓通知渠道（阶段2）
文件与分享（阶段3） ──→ 登录态与幂等（阶段4）
vitest 单测（阶段5贯穿）
```

---

## 7. 下一步行动（今天就能做）

- [ ] 拍板技术栈（本方案推荐 uni-app；若你想走 Flutter/原生，方案需调整）
- [ ] 安装 HBuilderX → 新建 uni-app（Vue3）工程到本仓库目录
- [ ] 完成阶段 0，手机跑通"Hello App"
- [ ] 第一个 commit：项目骨架

> 本方案随项目进展迭代更新；任何调整请同步修改本文件并提交。
