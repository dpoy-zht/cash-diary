# 奶龙风视觉规范 v1.0

> ⚠️ **设计基准已变更（2026-09-26）**：用户提供了「奶龙记账-复现参考包」
> （`C:\Users\zhtzh\Doubao\chats\2026-09-26\new-chat\奶龙记账-复现参考包\`，含复现参考.md + 成品原型 + 5 张 milo 图），
> 要求**摒弃本文档所述的 v1.x 设计方案**。**当前唯一设计基准是 `demo/nailong-ledger.html`（v2.0）**，
> 它逐字复现参考包：蛋黄主色 `#FFD93D`、平涂奶油底 `#FFF8E7`、纯白卡、三页结构（首页/记一笔/统计）、
> 底部 5 Tab（含凸起 +）、4 个状态弹窗、分类图标 8 色严格一致。
> **同日 `src/`（真机 App）已按 v2.0 完成迁移**（新 IA：home/add/stats/me + 自定义 tab-bar；
> PRD §8 已升 v2.0）。本文档 §1–§12 描述的是**已被取代**的 v1.1/v1.2 方案，仅作历史参考。
>
> 日期：2026-09-25 ｜ 范围：纯视觉层（配色 / 圆角 / 字体 / 插画 / 动效）
> 本次不改变任何交互路径、数据流与存储逻辑。

---

## 1. 改动范围（页面与组件）

| 层级 | 文件 | 改动性质 |
|---|---|---|
| 令牌层 | `src/App.vue` | **新增**全站设计令牌（CSS 变量）+ 全局字体与共用关键帧 |
| 配置 | `src/pages.json` | 导航栏 / 底部 Tab / 页面底色换色 |
| 页面 | `src/pages/quick-add/quick-add.vue` | 重写样式；金额区包成卡片并嵌入吉祥物 |
| 页面 | `src/pages/transactions/transactions.vue` | 重写样式；月合计改双卡；按天分组改卡片；空状态嵌入吉祥物 |
| 页面 | `src/pages/me/me.vue` | 重写样式；新增吉祥物问候卡 |
| 组件 | `src/components/money-keyboard/money-keyboard.vue` | 重写样式；新增退格键弱化态与按下回弹 |
| 组件 | `src/components/category-grid/category-grid.vue` | 重写样式；图标底改为按分类取柔和底色 |
| 组件 | `src/components/tx-item/tx-item.vue` | 重写样式；图标底与宫格同色；金额放大加粗 |
| 组件 | `src/components/edit-sheet/edit-sheet.vue` | 重写样式；新增上滑入场与暖色遮罩 |
| 组件 | `src/components/mascot/mascot.vue` | **新增**吉祥物（纯 CSS，零图片资源） |
| 工具 | `src/utils/palette.js` | **新增**分类底色映射（纯展示层） |
| 原型 | `demo/archive/ledger-prototype-v1.2.html` | CSS 与令牌同步，新增吉祥物与 CSS 变量说明 |

**明确未改动**：`src/stores/`、`src/services/`、`src/db/`、`src/utils/money.js`、`src/utils/date.js`、`tests/` 中的既有用例。
页面 `<script setup>` 只增删了模板 class 绑定，业务函数体一字未动。

---

## 2. 视觉基调：从"奶龙"里提取什么

参考对象是《奶龙》（第七印象）的**风格语言**，不是照搬角色形象。提取出四条可工程化的特征：

1. **明亮温暖的黄色为主体色**——不是糖果荧光黄，是偏奶油/蛋黄的低饱和暖黄。
2. **一切皆圆润**——圆脑袋、胖身材的造型语言，投射到界面就是"没有直角"：卡片 20–28px 圆角，标签全用胶囊。
3. **黑亮圆眼 + 两朵红晕**——这是奶龙最可识别的面部特征，成为吉祥物的核心元素。
4. **温暖治愈而非冷硬**——用暖深棕替代纯黑做文字色，阴影带暖调，遮罩不发灰。

### 关于角色形象的处理方式（重要）

吉祥物 **「小记龙」是本次原创的**，只借鉴风格特征，不复制奶龙的具体形象：

- 采用纯 CSS 几何绘制（`<view>` + `border-radius` + 渐变 + 内阴影），**不引入任何图片/SVG 资源**。
  好处是：APK 体积零增长；H5 / App / 未来小程序三端通用（小程序不支持内联 `<svg>`）。
- 只做**头像式**（仅头部），不做全身：46px 的下限尺寸下，全身造型会糊成一团，只留头部才能保持清晰。
- 与真实 IP 的差异是刻意保留的：奶龙无耳朵、有长尾巴与短爪，本吉祥物改用一撮"呆毛"区分，避免与商业 IP 形象混淆（若将来公开发布，也不存在形象侵权风险）。
- 两个表情状态：`happy`（默认，用于记一笔金额区、我的页）/ `sleep`（眯眼打盹 + 浮动 + "z"，仅用于明细空状态）。

---

## 3. 配色体系

全部色值集中在 `src/App.vue` 的令牌块，页面与组件一律 `var(--cd-*)`，**不再出现硬编码色值**（唯一例外是吉祥物头部渐变，属插画本体）。

### 3.1 令牌表

| 用途 | 令牌 | 色值 | 说明 |
|---|---|---|---|
| 页面底色 | `--cd-bg` | `#fff8e7` | 奶油底 |
| 卡片 / 弹层 | `--cd-surface` | `#ffffff` | |
| 次级填充 | `--cd-cream` | `#fff3d6` | 分段控件底、危险按钮外的浅底 |
| 输入框内底 | `--cd-cream-2` | `#fffaee` | |
| 品牌主黄 | `--cd-primary` | `#ffd34d` | 导航栏、选中态 |
| 品牌浅黄 | `--cd-primary-lt` | `#fff0be` | 按下反馈底 |
| 品牌深金 | `--cd-primary-dk` | `#f0a500` | 选中环、描边、呆毛 |
| 主按钮渐变 | `--cd-grad-brand` | `180deg, #ffe98f → #ffc93c` | **全站唯一**的按钮/选中态渐变 |
| 主文字 | `--cd-ink` | `#4a3520` | 暖深棕，替代纯黑 |
| 次要文字 | `--cd-ink-2` | `#7e6849` | |
| 弱化 / 占位 | `--cd-ink-3` | `#b9a88f` | 不承载正文信息 |
| 分隔线 | `--cd-line` | `#f2e6ce` | |
| 腮红 | `--cd-blush` | `#ff9cae` | 吉祥物 |
| 吉祥物五官 | `--cd-face-ink` / `--cd-face-mouth` | `#3d2a17` / `#c2504c` | 仅 mascot.vue |
| 支出 | `--cd-expense` | `#e24b4a` | **沿用 PRD §8 冻结值** |
| 收入 | `--cd-income` | `#12924f` | **沿用 PRD §8 冻结值** |
| 危险底 / 危险字 | `--cd-danger-lt` / `--cd-danger-ink` | `#fdeceb` / `#b93b39` | |

### 3.2 为什么保留 #e24b4a / #12924f（而不是一起调暖）

这两个是**语义色**，不是装饰色：红支绿收是 PRD §8 冻结的产品规范，用户对金额方向的识别依赖它。
把它们调成暖色会削弱语义、且违反已冻结的文档。因此本次只换"氛围色"（底、容器、主色），**语义色原样保留**。

### 3.3 语义色的使用规则（本次新增的可访问性约束）

金额红绿在**白底**上的对比度是 3.94:1 / 4.00:1——达到 WCAG AA 的**大字号**门槛（≥3:1），但达不到正文字号的 4.5:1。
因此定死一条规则：

> **红 / 绿只出现在金额数字上，且金额一律 ≥19px 粗体。**

推论：
- 记一笔大金额 30px、明细条目金额 19px、月合计 19px —— 全部命中"大字号"档。
- 「支出 / 收入」这类小字标签一律用 `--cd-ink` / `--cd-ink-2`，不用红绿（分段控件的选中态改用品牌黄底 + 深棕字）。
- 唯一的红色小字场景（删除按钮）走加深档 `--cd-danger-ink`，对 `--cd-danger-lt` 为 4.90:1，满足正文 4.5:1。

### 3.4 分类图标底色

`src/utils/palette.js` 提供 8 个柔和底色，按**分类 id**（不是数组下标）循环取色：

```
#fff0be 奶黄 · #e6f6ff 天空蓝 · #ffedf0 腮红粉 · #e8f8f0 薄荷绿
#fff1e3 奶油橙 · #ede9ff 淡紫 · #e4f5f7 淡青 · #fbeaf2 淡玫
```

内置分类 id 为 1..12（8 支出 + 4 收入），8 色一循环后：**支出 8 项颜色两两不同，收入 4 项也两两不同**。
按 id 取色（而非按位置）是为了让同一分类在宫格、明细列表、编辑弹层 chip 三处颜色完全一致。

---

## 4. 一致性原则

### 4.1 圆角

| 令牌 | 值 | 用途 |
|---|---|---|
| `--cd-r-pill` | 999px | 标签、chip、把手 |
| `--cd-r-lg` | 28px | 卡片、弹层顶角 |
| `--cd-r-md` | 20px | 按钮、输入框、分组卡、金额卡 |
| `--cd-r-sm` | 16px | 键盘键、分段控件内块 |

规则：**界面内不出现直角**；同类元素必须用同一个令牌，不允许"这里 10px 那里 12px"。

### 4.2 字体

- **不打包字体文件**（避免 APK 体积与首屏成本），使用系统圆体栈：
  `MiSans → HarmonyOS Sans SC → PingFang SC → Noto Sans SC → system-ui`
  测试机 Xiaomi 14 Pro / HyperOS 命中 **MiSans**，本身即圆润几何无衬线，天然贴合本风格。
- 字号档位：11 / 12 / 13 / 14 / 15 / 16 / 17 / 19 / 21 / 30px。
- 字重只用 400 / 600 / 700；标题与数字一律 700，避免"胖乎乎"的圆润造型配细字重。
- **所有金额数字**加 `font-variant-numeric: tabular-nums`，保证等宽、翻转与编辑时不跳动。

### 4.3 动效

| 场景 | 表现 |
|---|---|
| 键盘按键 | 按下 `scale(0.94)` + 底色转浅黄（180ms 轻回弹曲线） |
| 分类选中 | 图标底转主黄渐变 + 金色描边环 + `scale(1.06)` |
| 分类按下 | 图标 `scale(0.92)` |
| 主按钮 | 按下 `scale(0.985)` + 透明度 0.9 |
| 编辑弹层 | 底部上滑 240ms + 遮罩淡入 200ms |
| 空状态吉祥物 | 2.8s 循环上下浮动 8% |

统一缓动 `--cd-ease: cubic-bezier(0.34, 1.35, 0.64, 1)`（略带回弹，呼应软萌）。
`App.vue` 里已加 `@media (prefers-reduced-motion: reduce)` 全局降级，尊重系统"减弱动态效果"。

---

## 5. 对比度实测（WCAG 2.1）

| 组合 | 比值 | 结论 |
|---|---|---|
| `--cd-ink` 对奶油底 `--cd-bg` | **10.9:1** | AA / AAA 正文通过 |
| `--cd-ink` 对白卡 | **11.5:1** | AA / AAA 正文通过 |
| `--cd-ink` 对品牌黄 `--cd-primary`（导航栏、选中态） | **8.1:1** | 通过 |
| `--cd-ink-2` 对白卡 / 奶油底 | **5.3:1 / 5.0:1** | AA 正文通过 |
| `--cd-ink` 对按钮渐变最亮端 `#ffe98f` | **9.5:1** | 通过 |
| `--cd-expense` 对白卡（19px 粗体） | **3.94:1** | AA 大字号通过 |
| `--cd-income` 对白卡（19px 粗体） | **4.00:1** | AA 大字号通过 |
| `--cd-danger-ink` 对 `--cd-danger-lt`（删除按钮） | **4.90:1** | AA 正文通过 |
| Tab 选中字 `#8a5a0f` / 未选中 `#7e6849` 对 `#fffdf7` | **5.8:1 / 5.2:1** | 通过 |
| 禁用态文字 `--cd-ink-3` 对 `--cd-cream` | 2.1:1 | 低，但 WCAG 1.4.3 明确豁免禁用控件 |

触达面积：键盘键高 50px、主按钮 50px、弹层按钮 46px，均 ≥44px（WCAG 2.5.5）。

---

## 6. 改动后请重点查看的界面与状态

建议按此顺序看（`demo/archive/ledger-prototype-v1.2.html` 可直接在浏览器交互，与真机表现一致）：

| # | 界面 / 状态 | 怎么进入 | 看什么 |
|---|---|---|---|
| 1 | 记一笔 · 初始态 | 打开即默认页 | 黄色导航栏与奶油底是否协调；「完成」按钮的禁用态是否够"未激活" |
| 2 | 记一笔 · 输入中 | 按数字键 + 点分类 | 金额转红、分类选中出现金色环；键盘按下回弹；「完成」变亮黄实心 |
| 3 | 记一笔 · 切到收入 | 点「收入」 | 金额是否转绿；分类宫格整体换组；原先选的分类被清空 |
| 4 | 明细 · 有数据 | 底部「明细」 | 月合计双卡（红/绿）；按天分组的卡片；分类图标底色与宫格是否一致 |
| 5 | 明细 · 空状态 | 左右切到没有记录的月份 | 打盹吉祥物 + "z" + 浮动动画；文案与留白 |
| 6 | 编辑弹层 | 点任意一条流水 | 弹层上滑动画；遮罩是否偏暖而不发灰；chip 选中变黄；删除按钮的加深红 |
| 7 | 删除二次确认 | 弹层内点「删除」 | 系统确认框仍正常弹出（此为功能验证，样式不可控） |
| 8 | 我的 | 底部「我的」 | 吉祥物问候卡；P1/P2 胶囊标签；版本号弱化色 |
| 9 | 切换 Tab | 底部三栏 | 选中字色为深棕金，是否够清晰但不刺眼 |
| 10 | 长金额溢出 | 输入 999999999 | 金额右侧对齐、不撑破卡片（超长会裁切左侧，属预期） |

**真机专项**（HBuilderX 运行到小米 14 Pro）：
- 系统导航栏是否为品牌黄、文字为黑（`pages.json` 的 `navigationBarBackgroundColor`）。
- 系统 TabBar 选中色是否为深棕金（`selectedColor: #8a5a0f`，TabBar 由原生绘制，与 H5 表现不同）。
- 键盘、分类宫格在真实触屏上的回弹手感。
- 全面屏底部安全区（编辑弹层底部留白是否被手势条遮挡）。

---

## 7. 功能与交互未受影响的证据

- 数据三铁律未触碰：金额仍是整数分、时间仍是毫秒时间戳、删除仍是软删除。
- `utils/money.js` / `utils/date.js` 未改一行，页面依旧不自行换算。
- 全部组件 props / emits / 事件名保持不变；模板改动仅为 class 绑定与容器包裹。
- 测试：`npm run test` **4 文件 35 用例全绿**（原 29 用例 + 新增 `tests/palette.test.js` 6 用例）。
- 编译：`npm run build:h5` 与 `npm run build:app` 均通过；已确认令牌在两端产物中均保留（H5 编译为 `:root,uni-page-body`，App 端编译为 `:root,body,uni-page-body`），不存在 `var()` 失效导致白屏的风险。

---

## 8. 已知取舍与后续可选项

| 项 | 现状 | 可选的下一步 |
|---|---|---|
| 支出红 / 收入绿 | 保留 PRD 冻结值，靠"≥19px 粗体"达到 AA 大字号 | 若要正文级达标，可加深为 `#c93a38` / `#0f7a42`（约 5.0 / 5.4:1），但需同步改 PRD §8 |
| 字体 | 系统字体栈，不打包 | 若要更强"圆体"感，可子集化打包一款圆体（约 +30–100KB） |
| 分类图标 | 仍用 emoji，只换了底色 | P1 分类管理时可换成统一的扁平图标集 |
| 深色模式 | 未做（PRD 列为 P2） | 令牌层已就绪，只需新增一组 `[data-theme="dark"]` 覆盖 |
| 吉祥物 | 仅头部、两个表情 | 可扩展 `mood`（如 `cheer` 记账成功后欢呼）与全身版用于启动页 |

---

## 9. 附：形象中心（真实素材 Demo）

`demo/archive/nailong-gallery-v1.1.html` —— 一个把「奶龙」**真实图片素材**嵌进 UI 的独立设计稿，用来验证
"带图片的信息型界面"的布局、按钮层级与加载体验。它**不属于 App 发布路径**（`src/` 未改动一行）。

### 9.1 版权前置约束

- 「奶龙」版权归第七印象文化传媒（深圳）有限公司，素材取自萌娘共享。
- 仅限本机原型验证与个人学习，**不得商用/公开发布/再分发**。
- 因此 **图片文件不入库**（`.gitignore` 已排除），运行 `demo/assets/nailong/fetch.py` 本地生成。
- 要正式上线，必须换成自有/已授权素材，或直接用项目自带的原创吉祥物 `src/components/mascot/`。
- 界面底部常驻「素材说明」卡片，把来源与限制写在用户看得到的地方。

### 9.2 布局与栅格

```
┌─ 顶栏（品牌黄，标题 + 主题标签）────────────┐
│ Hero 当前形象    236px 高，图 + 名称 + 描述 + 使用中徽章 │
│ ─ 分区标题「选一个形象」 + 计数 ─             │
│ 网格 2 列 · 间距 12 · 卡片 3:4 图 + 名称        │
│ ─ 文字按钮「恢复默认形象」─                  │
│ 素材说明卡片                                │
└─ 吸底操作栏（次按钮 + 主按钮）──────────────┘
```

- 单列 430px 上限，左右统一 20px 安全边距；纵向节奏 = 16 / 22 / 12px。
- 网格用 `aspect-ratio: 3/4` 锁死卡片比例，**图片未加载时也不会跳版**。
- 选中的唯一标识是"金色描边环 + 右上角勾"，不改变盒子尺寸（用 `box-shadow` 画环），因此不会引起重排。

### 9.3 两类素材两种处理（关键）

素材实测分两类，用同一套展示方式必然有一类难看，所以按类分流：

| 类型 | 特征 | 处理 | 底 |
|---|---|---|---|
| `cutout` 白底角色图（4 张） | 外圈 99%+ 纯白 | `fetch.py` 从四边**泛洪抠白底**成透明通道；`contain` + 12px 留白 | 柔和底色（贴纸感） |
| `poster` 满幅海报（5 张） | 边缘为天空蓝等彩色 | 保留原背景；`contain`（海报本身即 3:4，等价满铺不裁切） | **取画面边缘色**，无缝衔接 |

抠图只从四边泛洪，所以角色**内部的白色高光/肚皮不会被误伤**；紧贴边缘的一圈近白像素按白度递减 alpha，消除白边。

### 9.4 按钮交互逻辑（三级）

| 层级 | 控件 | 行为 | 状态 |
|---|---|---|---|
| 主 | 设为我的形象 | 写入 `localStorage`，Toast，Hero 出现「使用中」徽章 | 已应用 → 变「✓ 已应用」+ 薄荷底 + 禁用 |
| 次 | 查看大图 | 打开灯箱预览**当前选中**项 | 始终可用 |
| 弱 | 恢复默认形象 | 清掉存储、选中与已应用回到第 1 项 | 常驻文字按钮 |
| 内容 | 网格卡片 | 点选即改 `state.selected` → Hero 与按钮态同步 | `aria-pressed` 表达选中 |
| 模态 | 灯箱 | 左右箭头 / 圆点 / Esc / ←→ / 横向滑动（>45px） | 点遮罩或 ✕ 关闭 |

单向数据流：UI 只由 `state.selected` / `state.applied` 渲染；`syncSelection()` 只改属性、**不重建网格**，
所以已加载的图片不会被丢掉，点选手感是"跟手"的。

### 9.5 图片加载策略

1. 网格只加载 `<slug>-thumb.webp`（单张 5–24KB），带 `loading="lazy"` + `decoding="async"`。
2. 先挂**半透明白色微光骨架屏**（在浅奶黄与天蓝海报上都成立），`onload` 后淡入缩放到位。
3. 失败显示占位与 `fetch.py` 提示，而不是破图——**素材缺失时界面依然完整可用**。
4. 灯箱**先秒出已缓存的缩略图**，再后台加载大图，完成后淡入替换；并发用序号令牌防抖，快速连点不会串图。
5. 大图只在打开灯箱时请求，首屏不付这个代价。

### 9.6 素材优化实测

| 指标 | 数值 |
|---|---|
| 原始 PNG/JPG | 9 张 / 2584 KB |
| 缩略图（420px WebP q82） | 9 张 / **150 KB**（原图的 5.8%） |
| 大图（1000px WebP q84） | 9 张 / 267 KB |
| 抠图比例 | 4 张白底图各去掉约 60–64% 画面 |

### 9.7 验收清单

| # | 状态 | 怎么看 |
|---|---|---|
| 1 | 初始 | 首项选中（金环 + 勾），Hero 显示同图，主按钮可用 |
| 2 | 切换选择 | 点其它卡片 → 金环与勾转移，Hero 与描述同步换；主按钮恢复可用 |
| 3 | 应用 | 点「设为我的形象」→ Toast、Hero 出现「使用中」、主按钮变「✓ 已应用」并禁用 |
| 4 | 持久化 | 刷新页面 → 上次应用的形象仍是已应用态 |
| 5 | 灯箱 | 「查看大图」或点卡片外区域；箭头/圆点/Esc/←→/滑动 都能换图与关闭 |
| 6 | 加载态 | 断网或把 `assets/nailong` 改名 → 骨架屏转占位提示，布局不塌 |
| 7 | 两类素材 | 抠图项在浅色底上无白边；海报项与画框底色衔接自然 |

---

## 10. 吸顶导航：半透明 + 毛玻璃，以及它的降级链

适用文件：`demo/archive/nailong-gallery-v1.1.html` 的 `.app-bar`（顶栏）与 `.actionbar`（吸底操作栏）。

### 10.1 先决条件：顶栏必须真的"吸顶"

原实现的 `.app-bar` 是 flex 布局里的普通一项，内容在独立滚动区里滚动，**永远不会从它下面经过**——
这种情况下做毛玻璃毫无意义。所以第一步是把顶栏改成覆盖式：
`position: absolute` + 滚动区 `padding-top: var(--bar-h)`，内容因此会从顶栏下方穿过，毛玻璃才有作用对象。

### 10.2 两档渲染，默认走保守档

| 档位 | 触发 | 顶栏 | 底栏 |
|---|---|---|---|
| **纯色（默认）** | 初始状态、以及所有降级路径的终点 | `var(--bar-solid)` 不透明 | `var(--foot-solid)` 不透明 |
| **毛玻璃** | JS 判定"支持 && 系统允许 && 实测帧率够用" | `rgba(...,0.72)` + `blur(12px)` | 同左 |

**默认渲染成纯色**是刻意的架构选择：CSS 里写的是安全档，由 JS 去"加能力"。
这样 JS 未执行 / 被禁用 / 抛错时，界面只会更清楚，不会出现半透明底色漏白。
两档共用一个开关 `.app.blur-ok`，**顶栏与底栏一起切**，避免"只降一半"。

### 10.3 文字可读性：把"不透明度下限"算出来

半透明顶栏最大的风险是文字对比度随下方内容变化。做法是**先算下限，再定透明度**。

设顶栏底色亮度 `L_bar`、文字亮度 `L_text`、下方内容亮度 `L_under`、不透明度 `α`，
则合成后亮度 `L = α·L_bar + (1-α)·L_under`，要求 `(L+0.05)/(L_text+0.05) ≥ 4.5`。

**浅色档**（底色品牌黄 `#ffd34d`，`L≈0.683`；文字 `#4a3520`，`L≈0.041`）
最坏情况是下方为纯黑（`L_under=0`）：解得 **α ≥ 0.527**。取 α=0.72 有很大余量。

**深色档**（底色 `#241d15`，`L≈0.013`；文字 `#f4ead8`，`L≈0.831`）
最坏情况是下方为纯白（`L_under=1`）：解得 **α ≥ 0.866**——"半透明"几乎被算没了。
这里用另一个手段解决：`backdrop-filter` 里的 **`brightness(0.5)`**，它先把下方的亮图压暗再合成。
压暗后 `L_under≈0.216`，同样的 4.5 门槛下 **α ≥ 0.49** 即可，于是深色档取 α=0.62 也安全。

> 结论：**深色模式的半透明顶栏必须配合 `brightness()` 使用**，否则要么文字不可读，要么只能做成近似不透明。

### 10.4 降级链（全部走同一个开关）

| # | 触发条件 | 结果 |
|---|---|---|
| 1 | `CSS.supports('backdrop-filter','blur(1px)')` 为假，或 `prefers-reduced-transparency: reduce` | 直接纯色 |
| 2 | `hardwareConcurrency ≤ 2` 或 `deviceMemory ≤ 2` | 初判纯色（阈值取很保守，不误伤中端机） |
| 3 | 首次滚动时实测 p75 帧间隔 > 28ms（≈36fps） | 自动降为纯色 + 一次性 Toast 说明 |
| 4 | 降级结论写入 `localStorage`，下次直接进纯色 | 不必每次重受一遍罪 |
| 5 | 浮层（灯箱）打开 | 两条栏暂停毛玻璃 |

第 3 条是**在用户真实滚动时采样**的：静态页面测不出滚动合成的开销。
取 **p75 而非平均值**，避免一次偶发卡顿就把整机判成低端。

### 10.5 多层浮层

灯箱打开时给 `.app` 加 `has-modal`，暂停两条栏的 `backdrop-filter`。
两条栏已被 90% 深色遮罩完全盖住，**视觉上无法分辨**，但省掉全屏回读合成——低端机上很实在。

> ⚠️ 踩坑记录：这条"暂停"规则**必须写在两条栏的 `.blur-ok` 规则之后**。
> 同为 3 个类的选择器，同权重下靠后者生效；写在前面会被后面的规则盖掉，
> 表现为"顶栏停了、底栏还在模糊"的半吊子降级。已由计算样式实测抓出并修复。

### 10.6 深色模式

顶栏与底栏的深色适配不是一个特例，而是顺带完成了整套深色主题：

- 主题只由 `<html data-theme>` 一个开关驱动（JS 默认跟随系统，`?theme=dark|light` 可强制），
  **不写 `@media (prefers-color-scheme)`**，避免"媒体查询 + 属性"两套值各维护一遍。
- 新增 `--cd-on-accent`（叠在品牌黄上的字）。**这是深色模式最容易翻车的地方**：
  主按钮原本用 `--cd-ink`，深色下它会变成浅色，压到亮黄上对比度崩到约 1.5:1。
  凡"亮黄底 + 文字"的地方一律改用 `--cd-on-accent`。
- 卡片底色是按素材算出来的，换主题必须**重算**：浅色 tint 直接搬到深色下会刺眼，
  因此深色时把原色向深底混合（保留色相、压暗明度）。

### 10.7 实测结果

**文字对比度**（从渲染结果取样计算，内容滚至顶栏下方的最坏情况）：

| 主题 · 档位 | 文字色 | 顶栏实测底色 | 对比度 |
|---|---|---|---|
| 浅色 · 毛玻璃 | `(74,53,31)` | `(227,204,115)` | **7.22:1** |
| 浅色 · 纯色 | `(74,53,32)` | `(255,211,77)` | **8.06:1** |
| 深色 · 毛玻璃 | `(244,234,216)` | `(43,35,29)` | **12.94:1** |
| 深色 · 纯色 | `(244,234,216)` | `(36,29,21)` | **13.96:1** |

四档全部 ≥ 7:1，远超 AA 正文 4.5:1。

**两档是否真的不同**（同一点位，顶栏后方换成亮/暗内容）：

| 档位 | 亮背景下的顶栏 | 暗背景下的顶栏 | 最大通道差 |
|---|---|---|---|
| 纯色 | `(255,211,77)` | `(255,211,77)` | **0** → 不透明 |
| 毛玻璃 | `(255,221,119)` | `(204,202,123)` | **51** → 半透明生效 |

**运行时计算样式**（`--dump-dom` 读取）：

```
浅色/毛玻璃 | bar=blur(12px) saturate(1.35) | foot=blur(12px) saturate(1.35) | barBg=rgba(255,211,77,0.72)
深色/毛玻璃 | bar=blur(12px) saturate(1.2) brightness(0.5) | barBg=rgba(36,29,21,0.62)
深色/浮层开启 | bar=none | foot=none
浅色/纯色(回退) | bar=none | foot=none | barBg=rgb(255,211,77)   ← alpha=1，确实不透明
--cd-scrim（两套主题）= rgba(38, 26, 12, 0.9)
```

### 10.8 验收清单

| # | 怎么看 | 期望 |
|---|---|---|
| 1 | 首次进入 | 顶栏为不透明品牌黄（默认保守档），无半透明漏白 |
| 2 | 向下滚动，让彩色海报经过顶栏下方 | 顶栏开始"吃"到下方颜色（浅色下会变暗变浊），文字始终清晰 |
| 3 | 调试面板把顶栏切到「纯色」 | 立刻变成完全不透明纯黄，滚动时颜色纹丝不动 |
| 4 | 切「毛玻璃」 | 恢复半透明，滚动时随内容变化 |
| 5 | 打开大图预览 | 顶栏与底栏一起暂停模糊（被遮罩盖住，肉眼看不出差别） |
| 6 | 调试面板切「深色」 | 整套界面转深色：顶栏深底浅字、卡片底色自动压暗、主按钮仍为亮黄深字 |
| 7 | 低端机 / 模拟降级 | 可用 `?theme=` 与调试面板验证；帧率不足时会 Toast 告知并记忆 |
| 8 | 系统开启「减弱动态效果 / 弱化透明度」 | 直接进纯色档 |

> 调试面板（「外观调试」）是**为验证本节而加的**，正式产品应整段移除。

---

## 11. App 本体：自绘吸顶导航的落地

前述 §10 是在独立 HTML 设计稿里验证的机制；本节记录把它落进 uni-app 工程（`src/`）的做法。
涉及文件：`src/pages.json`、`src/components/nav-bar/nav-bar.vue`、
`src/utils/nav-tier.js`、`src/utils/nav-scroll.js`、`src/stores/ui.js`、三个页面。

### 11.1 为什么必须换成自绘导航

App 端的原生导航栏由系统 / WebView 外壳绘制，**页面内容无法滚到它下面**，
所以原生栏永远做不出半透明 + 模糊。唯一办法是：

```jsonc
// src/pages.json
"globalStyle": { "navigationStyle": "custom", ... }
```

> 校验方式：uni-app 会把它编译成 `navigationBar.style: "custom"`（注意不是同名键，
> 直接 grep `navigationStyle` 在产物里是找不到的，容易误判成"没生效"）。

三个页面的 `navigationBarTitleText` **保留**：自绘栏不读它，但它记录了每页应有的标题，
且将来若改回原生栏它仍是必需的。

### 11.2 组件结构：固定层 + 占位层

`nav-bar.vue` 的根节点是一个普通流内元素，内部两层：

```
.nav-wrap          ← 在文档流里，高度 = --cd-nav-total
├─ .nav            ← position: fixed，真正吸顶、内容从它下面经过
└─ .nav-ph         ← 占位层，把页面内容顶下来
```

**这样设计的好处**：页面只需在模板最顶部放一个 `<nav-bar title="…" />`，
不用自己算 padding。滚动时占位层先滚走，之后内容就穿过固定层——
这正是"吸顶"成立的关键（少了占位层，内容会被固定层永久盖住顶部）。

高度用 uni-app 注入的 CSS 变量拼出来，H5 端自动为 0：

```css
--cd-nav-total: calc(var(--status-bar-height, 0px) + var(--cd-nav-h));  /* 44px */
```

### 11.3 降级链（两层，互相兜底）

**CSS 层**（声明式，不依赖 JS）：

| 条件 | 结果 |
|---|---|
| `@supports (backdrop-filter: blur(1px))` 不成立 | `.can-blur` 整段不生效 → 停在纯色 |
| `@media (prefers-reduced-transparency: reduce)` | 再次强制纯色 + 关掉模糊 |

**JS/Store 层**（`stores/ui.js` + `utils/nav-tier.js`）：

| 顺序 | 条件 | 结果 |
|---|---|---|
| 1 | 用户偏好 `solid` / `blur` | 听用户的（`blur` 优先于一切自动判定） |
| 2 | `CSS.supports` 不支持 | 纯色 |
| 3 | 系统「减弱透明度」 | 纯色 |
| 4 | `hardwareConcurrency ≤ 2` 或 `deviceMemory ≤ 2` | 纯色（阈值保守，不误伤中端机） |
| 5 | 滚动实测 p75 帧间隔 > 28ms | 纯色 + Toast 告知，并写入存储记忆 |
| 6 | 全过 | 毛玻璃 |

**默认渲染成不透明纯色**，判定通过后才加 `.can-blur`。JS 未执行 / 抛错时只会更清楚，不会漏底色。

### 11.4 把降级逻辑做成可单测的纯函数

`utils/nav-tier.js` 只放判定（`decideNavTier` / `percentile` / `isFrameBudgetOk` / `isLowEndHint`），
不碰 uni / DOM，于是 `tests/nav-tier.test.js` 能把**每个分支**都钉住（29 个用例）——
这类"降级逻辑"最大的风险就是某个分支永远没被走到、上线才发现退不回去。

两条判定习惯：
- **取 p75 而不是平均值**：一次偶发卡顿不该把中端机判成低端。
  测试里固化了两组：8 帧里 1 帧卡 → 判达标；8 帧里 5 帧卡 → 判不达标。
- **样本不足时返回"达标"**：证据不够 ≠ 有问题，不该据此降级。

### 11.5 顺带必须改的布局

明细页原来是"自己撑 `height: 100vh` + 内部滚动区"，那样流水**永远不从导航下方经过**，
毛玻璃在最有价值的一页上反而没有作用对象。因此改为**整页滚动**：

| 改动 | 原因 |
|---|---|
| `.page` 去掉 `height: 100vh` 与 flex 列 | 让页面成为普通文档流，整页滚动 |
| `<scroll-view scroll-y class="list-scroll">` → `<view class="list-scroll">` | 去掉多余的滚动容器，避免"双层滚动" |
| 去掉 `flex: none` / `flex: 1` 等只为旧 flex 列服务的声明 | 清理死代码 |

副作用：月份切换栏不再常驻（会随页面滚走）。**这是刻意的取舍**——
换来的是内容能穿过吸顶导航、且全局只有一个滚动层（不会有嵌套滚动的"滚不动"感）。

### 11.6 实测（H5 构建产物 + headless Chrome，500css px 视口）

| 检查项 | 结果 |
|---|---|
| 自定义导航是否生效 | App 端产物 `navigationBar.style: "custom"` ✓ |
| 导航是否处于毛玻璃档 | 实测 `(255,221,119)`，与"72% 半透明叠在奶油底 `(255,221,120)`"吻合 ✓ |
| 是否真的"跟随背后内容" | 滚到白卡上方时导航变 `(254,222,126)`，蓝通道 119→126 ✓ |
| 内容是否穿过导航下方 | 明细页滚动后首张卡片被导航截断 ✓ |
| 是否只有一个滚动层 | 明细页整页滚动，无嵌套滚动 ✓ |
| 数据聚合是否仍正确 | 月合计 `¥2,815.40 / ¥8,700.00`，与 22 条演示数据逐项加总一致 ✓ |
| 令牌是否进产物 | H5 与 App 端均含 `--cd-nav-*`（含 `calc(var(--status-bar-height))`）✓ |
| `@supports` 是否保留 | 三个页面的产物 CSS 各保留 1 处 ✓ |
| 单测 | `npm run test` **5 文件 64 用例全绿**（新增 `nav-tier.test.js` 29 用例） |

### 11.7 真机需人工确认（我无法代验）

H5 端 `--status-bar-height` 为 0，所以下面这些只能在真机上看：

| # | 检查点 | 期望 |
|---|---|---|
| 1 | 状态栏是否被让出来 | 标题不顶到状态栏（刘海/挖孔不遮挡文字） |
| 2 | 导航黄是否与状态栏连成一片 | 状态栏区域也是品牌黄（`.nav` 的底色向上延伸） |
| 3 | 明细页滚动 | 流水穿过导航下方；无"滚不动"或双层滚动 |
| 4 | 低端机降级 | 滚动是否掉帧；掉帧会 Toast 提示并记住，下次直接纯色 |
| 5 | 与原生 TabBar 的关系 | TabBar 仍由原生绘制，选中色 `#8a5a0f` |
| 6 | 编辑弹层层级 | 弹层（z-index 99）应盖住导航（z-index 90） |

### 11.8 已知取舍

| 项 | 现状 | 说明 |
|---|---|---|
| 深色模式 | 未做 | App 端需要"跨页面的主题开关"才谈得上，与设计稿里那套 `data-theme` 不是一回事；令牌层已就绪，属独立任务 |
| 状态栏背景色 | 靠 `.nav` 底色向上延伸 | 若真机上状态栏区域不是黄色，需再给 `.nav-status` 单独设底色 |
| 月份栏不常驻 | 见 §11.5 | 若更看重"随时切月份"，可把它做成导航下方第二层 `sticky`，但会多一层合成开销 |

---

## 12. 视觉改造：对齐 sdufe-nailong-checkin（马卡龙奶油玻璃风）

> 参考仓库：https://github.com/dpoy-zht/sdufe-nailong-checkin （单文件 HTML，2023 行，自带 10 张奶龙图）
> 它的 `:root` 自称"奶龙奶油卡通风主题变量 · 修改以下变量即可一键换肤"——
> 因为对方把整套视觉收敛成了变量，本次才可能"换皮不改结构"。

### 12.1 两套风格的差异（为什么要改这么多）

| 维度 | 改造前（奶龙风 v1.1） | 改造后（对齐参考项目） |
|---|---|---|
| 配色 | 单主色饱和黄 `#FFD34D` | **马卡龙多色**：鹅黄/蜜桃/薄荷/樱花粉/天蓝/薰衣草 |
| 页面背景 | 平涂奶油 `#FFF8E7` | **三段多色渐变**（黄→粉→薄荷），`background-attachment: fixed` |
| 卡片 | 纯白 + 单层暖阴影 | **玻璃拟态**：半透明白渐变 + 白描边 + **多层阴影** + 顶部高光条 |
| 圆角 | 16 / 20 / 28 / 999 | **16 / 24 / 32 / 40 / 999**（整体放大一档） |
| 动效 | 单条轻回弹曲线 | **三条曲线**：弹性 `spring` / 平滑 `smooth` / 过冲 `bounce` |
| 字重 | 最高 700 | 最高 **800**（标题、金额、按钮全部加重） |
| 导航 | 平底品牌黄 | **鹅黄→蜜桃渐变 + 底部 32px 大圆角 + 标题带 IP 头像** |
| 字体栈 | 系统圆体 | 前置 `Nunito` / `Varela Round`（不联网加载，装机即用） |

### 12.2 改造范围（页面与组件清单）

| 层级 | 文件 | 具体改动 |
|---|---|---|
| 令牌层 | `src/App.vue` | 整套重写：马卡龙色板 / 玻璃材质 / 多层阴影 / 圆角 4 档 / 3 条曲线 / 导航令牌重算 |
| 全局材质 | `src/App.vue` | 新增可复用类 `.cd-card` `.cd-glow` `.cd-elevated` `.cd-glass` `.cd-press` |
| 组件 | `nav-bar.vue` | 渐变底 + 底部 32px 圆角 + 标题左侧吉祥物头像（对应参考项目 `.top-nav` / `.nav-icon`） |
| 组件 | `money-keyboard.vue` | 键改玻璃 + 白描边；按下转品牌渐变 + 弹性回缩 |
| 组件 | `category-grid.vue` | 容器改玻璃卡；图标加白描边；选中改用**加深档**渐变 |
| 组件 | `tx-item.vue` | 分隔线改暖棕半透明；图标加白描边；金额字重 800 |
| 组件 | `edit-sheet.vue` | 弹层玻璃化 + 顶角抬到 `--cd-r-xl`；输入框用更不透明的玻璃 |
| 组件 | `mascot.vue` | 头部渐变改令牌（`--cd-face-head`），从饱和黄调成马卡龙鹅黄系 |
| 页面 | `quick-add.vue` | 分段控件/金额卡/备注/日期全部玻璃化；主按钮对齐 `.btn-cream`（渐变+发光+弹性） |
| 页面 | `transactions.vue` | 月份圆钮、合计卡、按天卡全部玻璃化 |
| 页面 | `me.vue` | 问候卡（挂 `.cd-glow`）、两张设置卡玻璃化 |
| 工具 | `utils/palette.js` | 分类底色换成参考项目的马卡龙 6 色 + 2 支同族色 |
| 原型 | `demo/archive/ledger-prototype-v1.2.html` | 令牌块整体同步（老令牌名留兼容映射，25 处旧引用无需逐条改） |

### 12.3 设计规范怎么"复用"：三条通道

**通道一：令牌一对一映射**（改配色只动一处）

| 参考项目 | 本项目 | 说明 |
|---|---|---|
| `--cream-yellow` `#FFE4A0` | `--cd-cream-yellow` | 主色 |
| `--peach` `#FFD4B3` | （并入 `--cd-grad-brand`） | 渐变终点 |
| `--text-main` `#5A4A3A` | `--cd-ink` | 主文字，各底色 7.57–8.34:1 |
| `--text-sub` `#9A8B7A` | `--cd-ink-2` `#6B5D4F` | **刻意加深**，见 §12.4 |
| `--bg-page` | `--cd-bg` | 三段渐变 |
| `--bg-card` | `--cd-card` | 玻璃卡底 |
| `--shadow-contact/mid/far/inset` | `--cd-sh-contact/mid/far/inset` + `--cd-card-shadow` | 多层阴影 |
| `--radius-sm/md/lg/xl` | `--cd-r-sm/md/lg/xl` | 16/24/32/40 |
| `--ease-spring/smooth/bounce` | `--cd-ease` / `--cd-ease-smooth` / `--cd-ease-bounce` | 三条曲线 |

**通道二：材质抽成全局类**（这是"组件样式一致"的关键）

参考项目把玻璃配方在 `.status-card`、`.nailong-quote`、`.streak-bar`、`.timeline-item`、`.ach-cell`
上各写了一遍（它自己的 `::after` 选择器里列了 5 个）。本项目反过来做：
玻璃配方只在 `App.vue` 定义一次，组件只挂类名。

```html
<view class="amount-card cd-card cd-glow cd-elevated">   <!-- 焦点卡：+柔光斑 +更高阴影 -->
<view class="day-card cd-card">                          <!-- 列表卡：基础玻璃 -->
<view class="cat-grid cd-card">                          <!-- 容器卡 -->
```

| 类 | 作用 | 用在哪 |
|---|---|---|
| `.cd-card` | 玻璃底 + 白描边 + 多层阴影 + 顶部高光条 | 所有卡片容器 |
| `.cd-glow` | 右上角柔光斑 | 只用在大卡（金额卡、问候卡），列表里加会显脏 |
| `.cd-elevated` | 更高一档悬浮阴影 | 金额卡（页面视觉焦点） |
| `.cd-glass` | 更薄的玻璃 | 次级容器（分段控件、日期钮） |
| `.cd-press` | 弹性按压反馈 | 可点区域 |

> ⚠️ 不要用"在页面里覆盖 `box-shadow`"的办法做变体：`.cd-card` 与页面 scoped 规则同权重，
> 谁生效取决于样式注入顺序，属于不确定行为。变体一律在 `App.vue` 里加类（`.cd-elevated` 写在 `.cd-card` 之后，顺序确定）。

**通道三：交互语言照搬**

参考项目的手感来自三件事，本项目逐一对应：

| 参考项目 | 本项目 |
|---|---|
| `.btn-cream:hover` `translateY(-2px)` + 阴影加深 | `.cd-press:active` 弹性回缩 |
| `.btn-cream:active` `scale(0.96) translateY(1px)` | 同上，曲线换 `--cd-ease` |
| `.menu-btn:hover` `scale(1.05) rotate(-2deg)` | 月份圆钮 `scale(0.92)` 回缩 |
| `.status-card:hover` `translateY(-2px)` + 更高阴影 | `.cd-elevated` 静态高一档 |

### 12.4 三处刻意偏差（照搬会出问题）

**① 次要文字必须加深。** 参考项目的 `--text-sub: #9A8B7A` 实测：

| 底色 | 对比度 |
|---|---|
| 奶白卡 `#FFFDF7` | 3.25:1 |
| 渐变中段 `#FFF0F5` | 3.00:1 |
| 薄荷端 `#E8F5F0` | **2.95:1** |
| 导航鹅黄 `#FFE4A0` | **2.66:1** |

全部不达 AA 正文 4.5:1。本项目改用同色系加深的 **`#6B5D4F`**，在包括导航渐变两端的所有底色上都 ≥4.64:1。

**② 卡片不加 `backdrop-filter`。** 参考项目每张卡都有 `backdrop-filter: blur(20px)`。本项目只保留在吸顶导航上，理由：

- 背景是**平滑多色渐变**，模糊前后肉眼几乎无差别（模糊一个渐变 ≈ 渐变本身）
- 但每张卡一次全屏回读合成是实打实的开销；明细页几十张卡，低端机直接掉帧
- 真正需要模糊的是"有内容从下方经过"的导航 —— 那里模糊才有作用

玻璃的观感其实主要来自**半透明 + 白描边 + 顶部高光 + 多层阴影**，这四项一个没少。

**③ 换底色必须重算导航不透明度下限。**
底色从 `#FFD34D`（L=0.683）换成更浅的鹅黄 `#FFE4A0`（L=0.793）/蜜桃 `#FFD4B3`（L=0.716）后，同一对比度需要更高的不透明度：

| 配色 | 下限 α（最坏：正下方纯黑） |
|---|---|
| 旧 `#FFD34D` + `#4a3520` | ≥ 0.527 |
| 新 鹅黄端 + `#5A4A3A` | ≥ 0.639 |
| 新 **蜜桃端** + `#5A4A3A` | **≥ 0.708** ← 取这端 |

故新导航取 **α = 0.84**（最坏 5.26:1）。
**只改配色不改这个参数，字就会看不清** —— 这是"换皮"最容易漏的一步。

### 12.5 实测

| 检查项 | 结果 |
|---|---|
| 令牌完整性 | 引用但未定义 **0** 个；定义但未使用 **0** 个 |
| 对比度 | 主文字 ≥7.57:1；次要文字 ≥4.64:1；导航毛玻璃最坏 5.26:1；语义红绿 ≥3.74:1 |
| 单测 | `npm run test` **5 文件 64 用例全绿**（palette 用例随色板更新仍通过） |
| 构建 | `build:h5` 与 `build:app` 均通过 |
| 视觉核对 | H5 产物 headless 截图逐页核对：记一笔 / 明细（含 22 条演示数据）/ 我的 / 原型 |

### 12.6 进阶优化建议

按"投入产出比"排序，分三组。

#### A. 视觉效果

| # | 建议 | 做法 | 优先级 |
|---|---|---|---|
| A1 | **时段主题背景** | 参考项目按 `body[data-period]` 切 5 组渐变（早/午/下午/晚/夜）。本项目只要在 `App.vue` 加 5 组 `--cd-bg` 覆盖，再由 `stores/ui.js` 按小时写入 | P1 |
| A2 | **夜间档顺带解决深色模式** | 参考项目的 night 档已给出深底 + `#B0BEC5` 文字（实测 6.90:1）。这是 App 端深色模式最省力的入口 | P1 |
| A3 | 扫光高光 | 参考项目 `.btn-cream::after` 的斜向 `skewX(-20deg)` 白光是 hover 扫过（参考项目 25-27 行）。移动端改到 `:active` 触发会打断按压手感，建议只给 H5/桌面端 | P2 |
| A4 | 金额专用数字字体 | 现在靠 `tabular-nums` 防跳字，但字形仍是系统字体。可子集化打包一款圆体数字（约 +20KB） | P2 |
| A5 | 收支用马卡龙功能色 | 参考项目有签到/离馆/签离三色（淡蓝/暖橙/柔粉）。可用同思路给"支出/收入"加浅底胶囊区分，把红绿彻底只留给数字 | P3 |
| A6 | 装饰元素 | 参考项目的漂浮云朵 / 边角贴纸 / 滚动跟随奶龙。**记账是高频输入场景，建议只放"我的"页或空状态**，不要进记一笔 | P3 |

#### B. 交互细节

| # | 建议 | 价值 | 优先级 |
|---|---|---|---|
| B1 | **按键触觉反馈** | 数字键盘加 `uni.vibrateShort()`。记账多为盲操作，触觉比视觉反馈更有效 | P1 |
| B2 | 长按退格连删 | 现在只能一下一下删，金额打错要按很多次 | P1 |
| B3 | 记账成功让吉祥物欢呼 | `mascot.vue` 的 `mood` 已预留扩展位，加 `cheer` 表情 + 弹一下，把"记完了"这个正反馈做足 | P1 |
| B4 | 明细左滑操作 | 左滑出现"编辑/删除"，比"点进弹层再操作"少一步；也避开弹层遮挡 | P2 |
| B5 | 月份切换支持滑动 | 现在只有左右箭头；加手势滑动 + 惯性更符合直觉 | P2 |
| B6 | 撤销 | Toast 里带"撤销"，误删/误记一笔可立刻回退（软删除已具备实现条件） | P2 |

#### C. 整体体验

| # | 建议 | 价值 | 优先级 |
|---|---|---|---|
| C1 | 首屏骨架屏 | DB 读取是异步的，现在首帧可能是空的。用 `.cd-card` 形状的骨架占位，避免闪一下 | P1 |
| C2 | 无障碍补完 | 金额加 `aria-label` 朗读、支持系统字号缩放、检查 TalkBack 顺序 | P2 |
| C3 | 明细虚拟列表 | 单月超过 ~200 条后长列表会拖慢渲染 | P2 |
| C4 | H5 版加 PWA | 参考项目有 `manifest.json` + `sw.js`；本项目 H5 预览版也可离线可用，顺带能"添加到主屏"当 App 用 | P3 |
| C5 | 常用金额快捷值 | 键盘上方加 2–3 个常用金额（如 15/30/100），进一步逼近"3 秒记一笔" | P3 |

> 建议先做 B1/B3/C1 三项：都不大，但分别补上"盲操作的反馈""完成的正反馈""首屏不闪"，体感提升最明显。



