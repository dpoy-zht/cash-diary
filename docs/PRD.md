# 奶龙记账 · 产品设计文档（PRD）v2.0

| 项目 | 内容 |
|---|---|
| 文档版本 | v2.0（界面按「奶龙记账-复现参考包」全面迁移：蛋黄奶油风 + 首页/记一笔/统计 + 自定义 5 Tab） |
| 日期 | 2026-09-24（v2.0 更新于 2026-09-26） |
| 代码仓库 | https://github.com/dpoy-zht/cash-diary |
| 相关文档 | `docs/技术方案.md`（技术设计）、`docs/nailong-ui-spec.md`（视觉规范 v1.0）、`PLAN.md`（路线图）、`demo/nailong-ledger.html`（v2.0 设计基准原型） |

---

## 1. 产品概述

### 1.1 一句话定义
一款"3 秒记一笔、随时看得清、数据不丢"的个人记账 App，先跑通 Android 单机版。

### 1.2 背景与动机
日常消费分散在扫码、外卖、交通等各种场景，事后很难回答"钱花哪了"。现有记账工具要么功能臃肿、要么强制注册登录，违背"随手记"的初心。本产品从最小闭环做起。

### 1.3 目标用户
-  primary：个人用户单机使用（开发者自用起步）
-  暂不覆盖：多人协作、家庭共享账本

### 1.4 核心价值主张
| 价值 | 含义 | 衡量方式 |
|---|---|---|
| 快 | 打开到记完 ≤ 3 步 / 3 秒 | 记账操作计时 |
| 稳 | 离线可用、数据不丢 | 杀进程重开数据完好 |
| 清楚 | 流水一目了然、改错容易 | 列表信息完备、编辑路径 ≤ 2 步 |

## 2. 产品原则

1. **闭环优先**：只做服务"记一笔 → 看得到 → 不丢"这条闭环的功能，其余一律后置。
2. **离线优先**：本地数据库是唯一事实源；没有网络也要能记账、看账。
3. **数据铁律**：金额用整数分存储；时间存毫秒时间戳；删除为软删除（详见 PLAN.md §3.3）。
4. **克制**：分类、字段、按钮能少则少；不为"以后可能用得上"提前设计。

## 3. MVP 功能范围（已冻结）

### 3.1 功能清单

| # | 功能 | 说明 | 验收标准 |
|---|---|---|---|
| F1 | 快速记账 | 数字键盘 + 收支切换 + 分类选择 + 备注（可空）+ 日期（默认今天） | 3 步内完成一笔 |
| F2 | 内置分类 | 8 个支出类 + 4 个收入类，带图标，启动初始化 | 不建分类管理界面 |
| F3 | 流水列表 | 按时间倒序、按天分组；按月查看，月头显示当月收支合计 |  grouping 正确 |
| F4 | 编辑 / 删除 | 点击条目弹出编辑层：改金额、分类、备注；可删除（二次确认） | 改删后列表与合计即时联动 |
| F5 | 本地持久化 | SQLite 存储（Demo 阶段以浏览器 localStorage 模拟） | 杀进程数据不丢 |

### 3.2 非目标（MVP 明确不做）

| 不做 | 原因 | 归属阶段 |
|---|---|---|
| 统计图表（饼图/趋势） | 不服务核心闭环；依赖稳定数据层 | P1 |
| 预算与超支提醒 | 依赖统计能力 | P1 |
| 分类管理（增删改排序） | 内置分类已够用 | P1 |
| 多账本、搜索、筛选 | 数据量小，用不上 | P2 |
| 云同步、账号登录 | 高风险区，后置 | P3/P4 |
| 深色模式、换肤 | 打磨项 | P2 |

## 4. 信息架构

> **v2.0 变更**：界面按「奶龙记账-复现参考包」迁移后，信息架构由「记一笔/明细/我的」三页
> 改为「首页/记一笔/统计/我的」四页，底部由原生三栏 Tab 改为**自定义 5 Tab**（含中间凸起 +）。
> 原「明细」页的功能（月份切换 + 按日流水）已并入首页。

```
奶龙记账
├── 首页（默认首页）
│   ├── 顶部：标题 + 金币（工资横幅演示）/ 铃铛（超支弹窗）
│   ├── 月份切换 ‹ 2026年9月 › + "本月你还可以花"
│   ├── 渐变余额卡：结余 / 已存(收入) / 已花(支出) + 吉祥物
│   ├── 分段：全部 | 支出 | 收入
│   └── 按日分组的流水列表（点击条目 → 编辑弹层：改金额/分类/备注、删除）
│   └── 右下 FAB(+) → 记一笔
├── 记一笔（推入页，带返回）
│   └── 收支切换 / 大金额 / 备注+日期 / 分类宫格 / 数字键盘（含"记好啦"）
│   └── 保存成功 → 弹窗「记好啦！」→ 引导回首页
├── 统计
│   ├── 月份选择器（独立于首页，可查任意月）
│   ├── 分段：日 | 周 | 月 | 年（v2.0 仅"月"已实现，其余提示规划中）
│   ├── 环形图卡：当月支出按分类分扇区，中心放吉祥物 + 图例
│   └── 排行卡：分类 ¥ 金额 + 占比条
├── 账本
│   ├── navbar：菜单 / 我的账本 / 新建
│   ├── 当前账本卡（财神奶龙圆形裁切趴右上角 + 账本名 + 累计结余 / 已存 / 已花）
│   ├── 其他账本（N）：当前数据层无多账本 → 诚实空态，不编造账本
│   └── 新建账本行（金条奶龙 + 黄色胶囊按钮）
└── 我的
    ├── 渐变头部（挥手奶龙 + 昵称 + 等级 + 「已坚持记账 N 天」胶囊）
    ├── 三统计卡：本月已花 / 累计已存 / 连续记账天数
    ├── 本月攒钱小目标卡（点卡片可设目标金额，进度条按累计已存算）
    ├── 功能列表 6 行：预算设置 / 分类管理 / 导出账单 Excel / 记账提醒 / 皮肤 / 关于
    ├── 底部问候卡 + 版权说明（第三方 IP 必须常驻）
    └── 重置数据（开发期工具，两次确认后清空全部流水并恢复内置分类）
```

底部 5 Tab：首页 / 统计 / **+（凸起）** / 账本 / 我的，五项均已接通真实页面。

> **与参考包的已知差异（都是"参考包写死假数据、这里换真数据"）**：
> ① 账本页「其他账本（3）」参考包是硬编码的 3 个假账本，本 App 数据层**没有 account 表**，
> 故为诚实空态——**多账本（新建/切换/各账本独立流水）是独立功能**，做完后版式不用动。
> ② 我的页的数字（¥3,286 / ¥12,580 / 21 天）参考包是写死的，这里全部为真实数据
> （本月已花、累计已存、连续记账天数按本地日计算、等级按累计笔数每 10 笔一级）。
> ③ 功能列表 6 行与参考包同名同位，点击给诚实提示（参考包里它们也只是 toast / 不可点）。

## 5. 核心流程

### 5.1 记账流程（主流程）
```
打开 App(默认在"首页")
 → 点右下 FAB 或底部凸起 + → 进入"记一笔"
 → 选择收支类型（默认支出）
 → 键盘输入金额（必填 > 0）
 → 点选分类（必填）
 → （可选）填备注、改日期
 → 点"记好啦" → 落库 → 弹「记好啦！」成功弹窗
 → 点"开心回家" → 返回首页，流水与余额即时更新
```

### 5.2 查看 / 编辑 / 删除流程
```
进入"首页" → 默认当月 → 可左右切换月份、按 全部/支出/收入 过滤
 → 点击条目 → 底部弹出编辑层
 → 修改金额/分类/备注 → 保存 → 列表与余额即时更新
 → 或点删除 → 二次确认 → 软删除 → 列表即时更新
```

### 5.3 异常与边界
| 场景 | 处理 |
|---|---|
| 金额为 0 或未选分类 | "完成"按钮禁用 |
| 删除操作 | 二次确认，防误触 |
| 某月无记录 | 空状态文案引导去记账 |
| 备注含特殊字符 | 转义后存储与展示 |

## 6. 页面详细说明

### 6.1 记一笔
| 元素 | 类型 | 规则 |
|---|---|---|
| 收支切换 | 分段控件 | 默认"支出"；切换后分类宫格随之切换，已选分类失效需重选 |
| 金额显示 | 大字右对齐 | 实时跟随键盘输入，千分位展示 |
| 数字键盘 | 3×4 宫格 | 0-9、小数点、退格；整数位 ≤ 9 位，小数位 ≤ 2 位 |
| 分类宫格 | 4 列图标宫格 | 显示当前收支类型的分类；选中高亮 |
| 备注/日期 | 输入行 | 备注可空；日期默认今天 |
| 完成 | 主按钮 | 金额>0 且已选分类才可点；点击后保留类型与分类便于连续记账 |

### 6.2 明细
| 元素 | 规则 |
|---|---|
| 月份栏 | "‹ 2026年9月 ›" 左右切换 |
| 月合计 | 支出红色总额、收入绿色总额（中国习惯：红支绿收） |
| 列表 | 按天分组（今天/昨天/M月D日 周X），组内按时间倒序 |
| 条目 | 分类图标 + 名称 + 备注（可空）+ 金额（带符号 ¥，支出 -、收入 +） |

### 6.3 编辑弹层（底部抽屉）
- 展示：类型（只读）、金额输入框、分类横排选择、备注输入框
- 操作：保存（校验金额>0、已选分类）、删除（二次确认）
- 日期不可改（MVP 范围外）

### 6.4 我的
- 仅占位入口（标记 P1/P2 敬请期待），含"重置演示数据"与版本信息

## 7. 数据模型（MVP）

```sql
category(id, name, type, icon, sort)          -- 内置，启动初始化
transaction_record(id, category_id, type, amount_cents,
                   note, occurred_at, created_at, updated_at, deleted_at)
```

- 金额一律整数分；时间一律毫秒时间戳
- `account`/`budget` 表不建（MVP 单账本、无预算）

## 8. 设计规范（v2.0 · 奶龙记账蛋黄奶油风）

> **v2.0（2026-09-26）** 界面按用户提供的「奶龙记账-复现参考包」全面迁移（`demo/nailong-ledger.html` 为逐字复现基准）。
> **信息架构随之变更**：三页 = 首页（余额卡+流水）/ 记一笔 / 统计；底部改为**自定义 5 Tab**（首页/统计/凸起 +/账本/我的），替代原生 tabBar。
> 复现基准与素材版权说明见 `demo/assets/milo/README.md`（milo 表情包为第三方 IP，**仅个人自用，上架前必须替换**）。

| 项 | 规则 |
|---|---|
| 主色（品牌） | 蛋黄 `#FFD93D`；余额卡/主按钮渐变 `#FFE082 → #FFD93D`；记一笔头部渐变 `#FFD93D → #FFE9A8` |
| 页面底色 | 奶油米白 `#FFF8E7`（平涂，非渐变）；卡片纯白 `#FFFFFF` |
| 文字颜色 | 主文字 `#5D4E37`；次文字/占位 `#B8A584`（对比度约 2.6:1，仅用于次要信息，按参考包原样保留） |
| 金额颜色 | 支出 = 主文字色（带负号），收入 = 绿 `#2E8B57`（带正号）——**v2.0 起取代红支绿收** |
| 分类体系 | **20 个**（12 支出 + 8 收入），对齐 v2.0 扩充版参考包。支出：早餐 / 午餐 / 零食 / 奶茶 / 公交 / 打车 / 购物 / 住房 / 娱乐 / 医疗 / 红包 / 其他；收入：工资 / 奖金 / 兼职 / 理财 / 红包 / 报销 / 二手 / 其他 |
| 分类图标 | **配色严格照参考包**：早餐 #FF8A65 / 午餐 #FF7043 / 零食 #FFB74D / 奶茶 #F48FB1 / 公交 #81C784 / 打车 #4FC3F7 / 购物 #FFC93C / 住房 #BA68C8 / 娱乐 #7986CB / 医疗 #4DD0E1 / 红包(支) #EF5350 / 其他 #A1887F / 工资 #AED581 / 奖金 #FFD54F / 兼职 #4DB6AC / 理财 #7986CB / 红包(收) #EF5350 / 报销 #64B5F6 / 二手 #CE93D8。白色面性 SVG 图标（CSS mask）渲染于彩色正圆底；老分类无映射时退回 emoji |
| 分类取色的键 | **必须按 `icon` 字段（存参考包 key，如 `breakfast`/`redbag`）取色，不能按分类名**——支出与收入里都有「红包」「其他」，按名字会撞车。改动见 `utils/palette.js` |
| 圆角 | 卡片 24px｜流水卡/弹层 20px｜键盘键 14px｜按钮/分段/图标钮 999px 胶囊｜图标正圆；全元素无尖角 |
| 阴影 | 暖黄发光 `0 8px 20px rgba(255,201,60,.3~.5)`（FAB/蛋黄按钮）+ 浅棕接触影（卡片） |
| 字体 | 系统圆润无衬线（PingFang SC / MiSans / 微软雅黑），字重 700–800，金额加粗 |
| 动效 | 弹性回弹 `cubic-bezier(.34,1.56,.64,1)` 180ms；弹层上滑 / 弹窗 pop；已适配"减弱动态效果" |
| 吉祥物 | App 内原创「小记龙」纯 CSS 绘制（我的页）；milo 表情包仅用于基准原型同款的余额卡/环形图/弹窗（第三方 IP，仅个人自用） |
| 危险操作 | 删除必须二次确认（系统弹窗） |
| 反馈 | Toast 轻提示 + 记好啦成功弹窗（milo-happy），不打扰流程 |
| 无障碍 | 点按目标 ≥44px；尊重"减弱动态效果"；次文字对比度为参考包已知取舍 |

> v1.1 / v1.2 历史规范见 git 历史与 `docs/nailong-ui-spec.md`（该文件顶部有"基准已变更"横幅）。

## 9. 验收标准（MVP 毕业线）

1. 打开 App 到记完一笔 ≤ 3 步
2. 连续记 10 笔，杀进程重开，数据一条不少
3. 编辑金额/分类/备注、删除流水后，列表与月合计即时正确
4. 明细按月分组清晰，月合计与明细加总一致（分毫不差——整数分校验）
5. 断网情况下以上全部可用

## 10. 后续版本展望

- **P1**：统计图表、预算提醒、分类管理
- **P2**：CSV 导出、JSON 备份恢复、多账本、搜索筛选、深色模式
- **P3/P4**：uniCloud 登录与多端同步、（远期）多人共享账本

> 本文档随版本迭代更新；功能变更需同步修改并提交。
